#### Description

Let's change something to trigger ci.

This is simple test typeScript project with Gulp task to build project.
Build project: 
    gulp
There is distribution files inside /dist folder after gulp build.
